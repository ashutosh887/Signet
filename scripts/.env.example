# Signet edge gateway / demo configuration.
# Copy to scripts/.env and edit.

# Where the verifier is reachable from the gateway and demo scripts.
SIGNET_VERIFIER=http://127.0.0.1:8000

# Port the gateway listens on for device triggers.
SIGNET_GATEWAY_PORT=8001
